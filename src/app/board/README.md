# `/board` — Pizarras

| Ruta | Uso |
|------|-----|
| `match/` | Partido (club / embebido en Sandbox) |
| `promo/` | Pizarra promo / monetización web solo si política lo permite |
| `training/` | Entrenamiento |

**Clase:** `match` y `training` → WEB-ONLY de facto (sin slots AdSense en código). `promo` → NATIVO-CANDIDATE en web.
