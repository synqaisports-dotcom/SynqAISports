# `/dashboard` — Operativa club

**Clase:** WEB-ONLY (sin AdSense web por política), salvo rutas explícitas en `src/lib/ads-policy.ts` (p. ej. `/dashboard/mobile-continuity`).

Incluye metodología, torneos, club, staff, sesiones y el segmento **`promo/`** que comparte pantallas con Sandbox vía reexports; las mismas páginas bajo `/dashboard/promo` **no cargan** `adsbygoogle` en web.
