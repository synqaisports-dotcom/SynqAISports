/**
 * Ancho normalizado (0–1) del icono de jugador en pizarras canvas (training / promo).
 * ~0.036 en un lienzo ~1100px ≈ 40px, alineado con PlayerChip (h-8…h-11 en partido).
 * El valor anterior 0.05 resultaba demasiado grande en campo.
 */
export const CANVAS_PLAYER_NORM_WIDTH = 0.036;
